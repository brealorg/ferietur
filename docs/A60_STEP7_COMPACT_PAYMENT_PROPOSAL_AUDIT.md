# A60 — Step 7 compact payment proposal

A60 removes repeated Step 7 explanation blocks and rebuilds the screen as a compact form:

- compact **Beregnet grunnlag** reference
- one Material radio group for calculated vs agreed amount
- custom amount/reason fields only when needed
- inline field validation instead of a large warning card
- one concise statement that an agreed amount does not change calculation or worktime warnings

Settlement state, persistence, next-step validation, settlement summary, finalized snapshot and PDF semantics remain unchanged. No calculation, tariff, worktime, payment, PDF or flow formula is changed.
